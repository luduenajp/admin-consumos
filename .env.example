# Database path (optional, defaults to ./data/app.db)
DB_PATH=

# CORS origins (optional, defaults to http://localhost:5173)
CORS_ORIGINS=

# Anthropic API key (optional, for comprobante analysis)
ANTHROPIC_API_KEY=

# Optional: basic auth credentials for the app
APP_USERNAME=
APP_PASSWORD=
